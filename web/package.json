{
  "name": "shopify-banner-app-web",
  "private": true,
  "scripts": {
    "debug": "node --inspect-brk index-new.js",
    "dev": "cross-env NODE_ENV=development nodemon index-new.js --watch ./",
    "start": "cross-env NODE_ENV=production node index-new.js",
    "build": "cd frontend && npm install && npm run build",
    "build:frontend": "cd frontend && npm install && npm run build",
    "postinstall": "cd frontend && npm install"
  },
  "type": "module",
  "engines": {
    "node": ">= 18.12.0"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cross-env": "^7.0.3",
    "express": "^4.18.2",
    "serve-static": "^1.15.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "prettier": "^2.8.7"
  }
}
